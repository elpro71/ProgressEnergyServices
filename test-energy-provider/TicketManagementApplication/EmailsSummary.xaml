<UserControl x:Class="TicketManagementApplication.EmailsSummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TicketManagementApplication"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:DebugConverter x:Key="DebugConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Tickets}"
            x:Name="dataGrid" Margin="23,24,0,0" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ticket Number" Binding="{Binding TicketNumber}"/>
                <DataGridTextColumn Header="Sender"/>
                <DataGridTextColumn Header="Mail summary" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
